# 茶色 → 入水 ロードマップ (Rust)

## 0. 目的地の定義

AtCoder のレーティング帯は 400 刻み。**水色は 1200〜1599** なので、当面の目標は
**レート 1200 に到達すること**。今いる茶色 (400〜799) からは 400〜800 の上積みになる。

ABC (Beginner Contest) で 1200 相当のパフォーマンスを出す、というのは実務的にはこうなる:

| | 今の想定 | 入水ライン |
|---|---|---|
| A・B | 確実 | 合わせて 5 分以内 |
| C | だいたい解ける | 10 分以内、詰まらない |
| D | たまに解ける | **8 割方 解ける**（ここが最大の伸びしろ） |
| E | ほぼ解けない | **3〜4 回に 1 回 解ける** |
| F 以降 | — | 解けなくてよい |

つまり入水は「難問が解ける」ことではなく、**D までを速く確実に、E を時々**。
新しい天才的発想ではなく、**典型の網羅と実装速度**が効く領域。

## 1. まず自分のボトルネックを特定する

茶色で止まる原因は 4 つに分かれる。次のコンテストで D が解けなかったとき、
どれだったかを必ず 1 行メモする。**対処法が全く違う**ので、混ぜて練習すると効率が落ちる。

| 詰まりの種類 | 症状 | 対処 |
|---|---|---|
| **知識不足** | 解説を読んで「そんなアルゴリズム知らない」 | §2 の必修リストを埋める |
| **考察不足** | 解説を読んで「それは思いつけたはず」 | 考察時間を長めに取る練習。実験・小ケース列挙・言い換えを手で書く |
| **実装力不足** | 方針は合っていたが書けない / バグって終わる | 同じ典型を何度も実装。ライブラリを整える |
| **速度不足** | 全部解けたが時間内に終わらない | バチャ (時間を計った過去問セット) を週 2 回 |

初期は圧倒的に **知識不足** が多い。まず §2 を潰す。

## 2. 必修アルゴリズム・データ構造

入水に必要なのはこれで概ね全部。**「名前を知っている」ではなく「見た瞬間に書ける」**が基準。

### 2.1 土台（ここが抜けていると全部崩れる）
- 計算量の見積もり — `10^8` 回/秒 を基準に、`N` から使える計算量を逆算する
- 全探索: 二重ループ / **bit 全探索** (`N ≤ 20`) / **順列全探索** (`N ≤ 8`)
- ソート + 貪欲、`sort_by_key` / `sort_unstable` の使い分け
- **二分探索** — 配列に対する `partition_point`、および**答えで二分探索**（判定問題に帰着）
- 累積和 / いもす法 / 二次元累積和
- 尺取り法、座標圧縮

### 2.2 グラフ
- DFS / BFS、グリッド上の探索、連結成分の数え上げ
- **Union-Find** → `ac_library::Dsu`
- **ダイクストラ法**（`BinaryHeap` + `Reverse`）
- 01-BFS (`VecDeque`)、ワーシャルフロイド (`N ≤ 400`)、ベルマンフォード（負閉路）
- 最小全域木（クラスカル法 = ソート + Dsu）
- トポロジカルソート、**DAG 上の DP**
- 二部グラフ判定

### 2.3 動的計画法（D・E で最も出る）
- ナップサック、部分和、個数制限付き
- LIS（最長増加部分列、`partition_point` で `O(N log N)`）
- bit DP（`N ≤ 20`、巡回セールスマン）
- 区間 DP（軽いもの）
- 遷移を「配る / もらう」どちらでも書けるようにする

### 2.4 数学
- エラトステネスの篩、素因数分解、約数列挙 (`O(√N)`)
- GCD / LCM（`num_integer::gcd`）
- **繰り返し二乗法**、mod 逆元、`nCr mod p`（階乗の前計算）
- → `ac_library::ModInt998244353` / `ModInt1000000007` で mod 演算はほぼ自動化できる

### 2.5 データ構造
- `BinaryHeap`（優先度付きキュー）、`BTreeMap` / `BTreeSet`（`range` で二分探索的に使う）
- **セグメント木** → `ac_library::Segtree`、**BIT** → `ac_library::FenwickTree`
- `rustc_hash::FxHashMap`（標準 `HashMap` は競プロでは遅い）

### 2.6 水色では基本いらないもの
遅延セグ木、ローリングハッシュ、Z-algorithm、最大流、SCC、Grundy 数、平方分割。
（ACL に入っているので使えるが、**優先度は低い**。§2.1〜2.5 を全部埋めてから。）

## 3. 教材（この順で消化する）

| 順 | 教材 | 目的 | 目安 |
|---|---|---|---|
| 1 | **競プロ典型 90 問**（★1〜★4 のみ） | 典型の網羅。入水に効く問題が集中している | 2〜3 か月 |
| 2 | **鉄則本（競技プログラミングの鉄則）** の演習 | 上と重なるが体系的。抜けの発見に使う | 並行 |
| 3 | **EDPC (Educational DP Contest) A〜L** | DP の型を身体に入れる | 2〜3 週間 |
| 4 | **ACL Practice Contest A・B・D・G** | ACL の使い方を実際に通す | 数日 |
| 5 | **ABC の C・D 埋め**（新しい回から遡る） | 本番形式での定着 | 継続 |

`ac_library` の使い方は素振りとして 4 を先に触ってしまってもよい（1 日で終わる）。

## 4. 日々の精進サイクル

**AtCoder Problems**（kenkoooo.com）を開き、Difficulty 表示を有効にする。

- **埋める難易度帯**: **400〜1000**。自分のレートより +200〜+400 が伸びる帯。
  1600 の問題を唸って解いても入水には直結しない。
- **1 日 1〜2 問**。毎日やる方が週末まとめてより効く。
- **考える時間は 30 分で打ち切る**。それ以上は伸びない。解説を読む。
- **解説 AC は「解けた」にカウントしない**。3 日後にもう一度、何も見ずに再実装して初めてカウント。

### 解けなかったときの手順（これを飛ばすと精進が精進にならない）
1. 解説を読み、**何を知らなかった / 何に気づけなかったかを 1 行で書く**（§1 の分類も付ける）
2. 解説を閉じて、自力で最後まで実装して AC を取る
3. その典型が出る別の問題を 1 問探して解く
4. 3 日後・1 週間後に再実装

### コンテスト当日
- **解けなかった最初の 1 問だけ**、終了直後に 30 分自力で粘る（記憶が新しいうちが一番伸びる）
- そのあと上の手順へ

## 5. 週のスケジュール例（週 8〜10 時間）

| | |
|---|---|
| 平日 ×5 | 1 日 1 問（30〜60 分）。§3 の教材を順に |
| 土 or 日 | **ABC 本番参加**（1 分でも遅れない。参加回数がレートの前提） |
| 参加後 | 復習（§4）— これを翌日に持ち越さない |
| もう 1 日 | **バチャ 100 分**（過去 ABC を 1 回分、時間を計って通しで） |

バチャは「速度不足」に唯一効く練習。実装が遅い自覚があるなら週 2 回に増やす。

## 6. Rust で戦うための素振り

言語で損をしないために、以下は**考えずに手が動く**状態にしておく。
（詳細な落とし穴は `README.md` の「Rust 固有のハマりどころ」を参照）

```rust
// 入力の型はこれだけ覚えれば ABC の 9 割が書ける
input! {
    n: usize,
    a: [i64; n],            // 長さ n の配列
    s: Chars,               // Vec<char>
    edges: [(usize, usize); m],
    grid: [Chars; h],
}
input! { p: [Usize1; k] }   // 1-indexed 入力を 0-indexed にして受け取る
```

- 迷ったら **`i64`**。`usize` の引き算は panic 源
- 出力が多い問題は `#[fastout]`（インタラクティブでは外す）
- `BinaryHeap` は最大ヒープ。ダイクストラでは `std::cmp::Reverse` で包む
- 探索の再帰が深いときはスタックを明示的に持つ

## 7. マイルストーン

| 段階 | 状態 | 目安 |
|---|---|---|
| M1 | ABC の **C を 10 分以内**で安定して通せる | 1 か月 |
| M2 | 典型 90 の ★2 を自力で 8 割 | 2〜3 か月 |
| M3 | ABC の **D が 8 割解ける**（＝ここで大体レート 1000 前後） | 4〜6 か月 |
| M4 | EDPC A〜L 完走、ACL を道具として使える | 5〜7 か月 |
| M5 | **E が時々解ける → 入水** | 6〜12 か月 |

期間は週 8〜10 時間・毎回 ABC に出る前提の目安。**参加回数が足りないと、実力があっても
レートは追いつかない**（レートは直近の成績に重み付けされるが、回数が少ないと補正で低く出る）。
まずコンテストに出続けることが最優先。

## 8. 進捗ログ

`log.md` に 1 行ずつ残す。振り返ったときに §1 の分類の偏りが見える。

```
2026-09-06 ABC431 ABC3完 D:WA→解説  [知識不足] 尺取り法を知らなかった
2026-09-08 典型90 #003 自力AC       木の直径
```
