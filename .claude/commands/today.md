---
description: 今日の精進問題を出す（精選100問の続き。解法には一切触れない）
disable-model-invocation: true
---

`python3 bin/pick` を実行する。

出力を**そのまま**表示する。問題文と制約を含めて全文。要約したり自分の言葉で
言い換えたりしないこと（問題文の改変は誤読を生む）。

**解法・方針・アルゴリズム名・計算量の見積もり・着眼点・コードを一切出さない。**
分野名（「二分探索」など）は出力に含まれているのでそのままでよいが、それ以上
踏み込まない。`CLAUDE.md` のヒント段階制に従う。

`bin/pick` がエラーで終わったら、エラー本文をそのまま出して終わる。
**別の方法で問題を選ばないこと**（勝手に選ぶとカリキュラムの順番が崩れる）。

末尾に 1 行だけ添える:

> ヒントは「ヒント」、実装は擬似コードを送る。終わったら `/log` で記録すると次に進む。
