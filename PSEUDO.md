# 擬似コード → Rust

移動中にスマホから実装するための書き方。**考えるのは本人、Rust の文法を埋めるのは Claude**、
という分担にする。方針を人に決めてもらったら精進にならないので、
**擬似コードには「どう解くか」が書かれていること**が条件になる。

書き方が思いつかない・方針が無いときは、これではなく `CLAUDE.md` のヒント段階制のほう。

## スマホでの流れ

1. 朝の通知から Claude を開く（問題文と制約はその画面にある）
2. 頭の中で解法を組み立てる
3. 擬似コードを送る。「abc467 c」のように問題を先に書く
4. Claude が `contests/<コンテスト>/src/bin/<記号>.rs` に書いて、コンパイルとサンプル判定まで回す
5. 結果が 1〜3 行で返る。WA なら落ちたケースが出るので、擬似コードを直して送り直す
6. Mac に戻ったら `git pull`。提出は自分でやる
7. `log.md` の結果に `(擬似)` を付ける

**Rust を自分で書かないぶん、実装力は伸びない。** 考察は自分でやっているので
「自力AC」ではあるが、`(擬似)` が続いたら週次振り返りで「Mac で 1 から書く回」が提案される。
移動中の 1 問と、腰を据えて書く 1 問は別物として数える。

## 書き方

**厳密でなくていい。** 1 行 1 命令、上から順。インデントは付けても付けなくてもよく、
ネストが分かればいい。記号が打ちにくければ日本語で書く（`>=` と「以上」は同じ）。
分からないところは日本語のまま書いてよい（「ここは BFS で最短距離」）。

| 書き方 | 意味 |
|---|---|
| `n 読む` / `n, m 読む` | 整数を読む |
| `a[n] 読む` | 長さ n の配列 |
| `s 読む（文字列）` | 文字列 |
| `h, w とグリッド g 読む` | h 行 w 列 |
| `n 回ループ` | 回数だけのループ |
| `i を 0..n でループ` | 添字つきループ |
| `〜 の間ループ` | while |
| `もし 〜 なら` / `そうでなければ` | 条件分岐 |
| `打ち切り` / `次へ` | break / continue |
| `ans 出力` / `Yes 出力` | 出力 |
| `dp[n+1] を 0 で用意` | 配列を作る |
| `集合 / 辞書 / キュー を用意` | HashSet / HashMap / VecDeque |
| `a を昇順ソート` | ソート |
| `a の合計 / 最大 / 最小 / 個数` | まとめて |

### はっきり書くべきこと

ここが曖昧だと Claude が推測で埋めてしまい、それは本人の考察ではなくなる。

- **添字が 0 始まりか 1 始まりか**（入力が 1 始まりなら「1 始まり」と書く）
- **答えが大きくなるか**（`10^9` を超えそうなら「答えは大きい」と書く）
- **出力の形**（`Yes/No` の大小、1 行ずつか空白区切りか）

変数名・ループの書き方・ライブラリの呼び方は適当でよい。

## 例

### 例 1

```
n, k 読む
a[n] 読む
cnt = 0
i を 0..n でループ
  もし a[i] が k 以上なら cnt を 1 増やす
cnt 出力
```

```rust
input! {
    n: usize,
    k: i64,
    a: [i64; n],
}

let mut cnt = 0usize;
for i in 0..n {
    if a[i] >= k {
        cnt += 1;
    }
}
println!("{}", cnt);
```

### 例 2

```
n, q 読む
a[n] 読む
s = a の累積和（s[0]=0、s[i] は a[0..i] の和）
q 回ループ
  l, r 読む（1 始まり、r も含む）
  s[r] - s[l-1] 出力
答えは大きい
```

```rust
input! {
    n: usize,
    q: usize,
    a: [i64; n],
}

let mut s = vec![0i64; n + 1];
for i in 0..n {
    s[i + 1] = s[i] + a[i];
}

for _ in 0..q {
    input! { l: usize, r: usize }
    println!("{}", s[r] - s[l - 1]);
}
```

### 例 3 — この粒度で通る

```
n 読む
h[n] 読む
dp[n] を すごく大きい値 で用意
dp[0] = 0
i を 1..n でループ
  dp[i] = dp[i-1] + |h[i]-h[i-1]| と dp[i-2] + |h[i]-h[i-2]| の小さいほう
  ※ i=1 のときは 1 個前だけ
dp[n-1] 出力
```

### 例 4 — 日本語のままでも通る

```
h, w とグリッド g 読む
s から t まで、上下左右に移動。'#' は壁
BFS で最短手数。届かないなら -1 出力
```

---

## 変換する側（Claude）の規則

**書かれたロジックだけを Rust にする。**

- アルゴリズムを別のものに置き換えない。計算量を勝手に改善しない。
  擬似コードが `O(N^2)` で制約に対して間に合わなくても、**そのまま実装して 1 行だけ指摘する**。
  直すのは本人の仕事（それが `ROADMAP.md` §1 の「考察不足」を潰す練習になる）。
- **ロジックに穴があっても埋めない。** `i=0` で `a[i-1]` を見ている、初期値が無い、
  といった穴は、**質問を 1 個だけして**返事を待つ。返事を待てない状況なら、
  仮定を明示して実装し、冒頭に「〜と仮定した」と書く。
- **擬似コードに解法が書かれていなければ変換しない。**「どうやって解くの」「方針を教えて」
  「BFS で解いて」程度しか無いものは、ヒント要求として `CLAUDE.md` の段階制で扱う。

### 補ってよいもの（考察ではなく作業なので）

- `input!` の形、出力の改行・空白
- 型（既定は `i64`、添字は `usize`）
- Rust 固有の安全化 — `usize` の引き算、`i32` の桁溢れ、`f64` の比較
- 定型のライブラリ呼び出し — ソート、`binary_search`、`HashMap`、`VecDeque`

補ったら、**コードの後に「Rust 側で補ったこと」を 3 行以内**で書く。
本人が「自分は何を書かなかったか」を分かるようにするため。

### 手順

1. 足場が無ければ `bin/new <コンテスト> <記号>`
   （クラウドは atcoder.jp に出られないが、`pool.json` のサンプルで作れる）
2. `contests/<コンテスト>/src/bin/<記号>.rs` に**書く**。チャットに貼るだけにしない
   （スマホでのコピペは手間なので、リポジトリに置くのが本体）
3. `bin/t <コンテスト> <記号>` でコンパイルとサンプル判定
4. 結果を短く返す。AC なら 1 行。CE ならエラーの要点だけ。
   **WA のときは `CLAUDE.md` の規則どおり**、落ちたケースを見せて本人に考えさせる
5. commit して push（`git checkout main` を先に）

### 出力の長さ

スマホで読むので、**コード全体を毎回チャットに貼らない**。変えた部分だけ数行見せて、
あとはファイルに書く。AC したらそれで終わり。
