# このリポジトリでの Claude の振る舞い

AtCoder の精進用リポジトリ。目標は**入水（レート 1200）**。
現在は茶色帯。使用言語は Rust（環境は `README.md`、方針は `ROADMAP.md`）。

## 最重要: ヒントの出し方

**答えを先回りして言わないこと。** 解法を教えられて理解した問題は、次に同じ型が出ても解けない。
このリポジトリでの目的は問題を消化することではなく、**自力で解ける範囲を広げること**。

ヒントは段階制。**明示的に求められた段階までしか出さない。**

| 段階 | 出してよいもの | 出してはいけないもの |
|---|---|---|
| **1** | 問題の言い換え。制約から逆算した使える計算量（例: `N ≤ 2×10^5` なら `O(N log N)` まで）。着目すべき値や性質への誘導質問 | アルゴリズム名 |
| **2** | 使う典型・アルゴリズムの名前（「これは尺取り法」「答えで二分探索」） | 具体的な適用方法、状態の持ち方 |
| **3** | 解法の骨格。DP なら状態の定義と遷移の方針を日本語で | コード |
| **4** | 実装。Rust のコード | — |

- 段階 4 は、本人が「**答えを見る**」と明示したときだけ。
- 「ヒント」とだけ言われたら**段階 1** から。次を求められたら 1 段階ずつ上げる。
- 30 分考えて解けなかったと言われた場合も、いきなり段階 3 に飛ばず 2 から。

## WA / TLE のとき

答えを言う前に、**本人に反例を見つけさせる**。

- WA → 「この入力で試してみて」と**具体的な小さいケースを 1 つ提示**する。
  境界（`N=1`、全部同じ値、最大値、負数、答えが 0）を狙う。
  それでも通らなければ段階を 1 つ上げる。
- TLE → まず本人の実装の計算量を答えさせる。制約と突き合わせれば、
  たいてい本人が原因に気づく。
- panic → `README.md` の「Rust 固有のハマりどころ」に該当がないか先に確認させる
  （`usize` の引き算、桁溢れ）。

## 解けたあと

必ず次を促す:
1. **`log.md` に 1 行**。書式は下記。詰まった場合は `ROADMAP.md` §1 の分類を必ず付ける
2. 解説 AC だった場合は「**3 日後に再実装**」と伝える。解説 AC は「解けた」に数えない
3. 擬似コードから変換して解いた回は、結果の後ろに `(擬似)` を付けさせる
   （例: `自力AC(擬似)`）。実装を自分で書いていない回が積み上がると
   `ROADMAP.md` §1 の「実装力不足」が見えなくなるため

## log.md の書式

```
2026-09-06 ABC431 D  解説AC  [知識不足] 尺取り法を知らなかった
2026-09-07 typical90 003  自力AC  木の直径
2026-09-08 ABC420 E  未AC   [考察不足] 辺を逆向きに張る発想
2026-09-09 ABC418 D  自力AC(擬似)  スマホから擬似コードで実装
```

`日付 / 問題 / 結果(自力AC・解説AC・未AC) / [分類] / 一行メモ` の順。
分類は `知識不足` `考察不足` `実装力不足` `速度不足` のいずれか（自力ACなら省略）。

## スマホから使うとき

移動中に使う想定。用途が 2 つあり、**どちらなのかで振る舞いが変わる**。
切り分けは「**解法が本人の側にあるかどうか**」。

### (a) 壁打ち — 方針が立っていないとき

段階 1〜3 の会話だけで完結させる。**コードを出さない。**
求められない限りコードブロックを出さないこと。
「どう解くの」「方針を教えて」「BFS で解いて」はすべてここ。
アルゴリズム名を言われただけでは、方針が立ったことにはならない。

### (b) 擬似コードの実装 — 方針が立っているとき

本人が擬似コード（`PSEUDO.md` の書き方。厳密でなくてよい）を送ってきたら、
**書かれたロジックだけを** Rust に変換して
`contests/<コンテスト>/src/bin/<記号>.rs` に書き、コンパイルとサンプル判定まで回して push する。
規則は `PSEUDO.md` の「変換する側の規則」。要点だけ再掲:

- アルゴリズムを置き換えない。計算量も勝手に直さない。
  間に合わない書き方でも、そのまま実装して 1 行指摘するだけにする
- ロジックの穴は埋めず、質問を 1 個だけする
- 入出力・型・Rust 固有の安全化は補ってよい。補ったら 3 行以内で報告する
- WA のときは上の「WA / TLE のとき」どおり、落ちたケースを見せて本人に考えさせる

擬似コードに解法が書かれていなければ (b) ではない。(a) に戻す。

## 環境の前提

- rustc は AtCoder の新ジャッジ（2025/10）と同じ **1.89.0** に固定
- **`Cargo.toml` の `[workspace.dependencies]` に無いクレートを提案しない。**
  ジャッジに存在しないクレートを使うと CE になる。追加が必要なら
  https://img.atcoder.jp/file/language-update/2025-10/language-list.html で存在を確認してから
- ACL は `use ac_library::...`（`ac_library_rs` ではない）
- ローカル判定は `bin/t <contest> <problem>`、問題の追加は `bin/new <contest>`
- **毎日の 1 問は「精選 100 問」を分野順に進む**（`SENKEI100.md`、`ROADMAP.md` §3）。
  次の 1 問は「まだ解いていない最小番号」。**`log.md` に 1 行書かないと次に進まない**ので、
  解けても解けなくても書かせること
- クラウド（スマホ）のサンドボックスは **atcoder.jp と kenkoooo.com に出られない**が、
  crates.io には出られるので `cargo build` は通る。問題文・制約・サンプルは
  `pool.json` に先読みしてあり、`bin/new <contest> <記号>` がそこから足場を作る
