# 精進ログ

`日付 / 問題 / 結果 / [分類] / 一行メモ` の順。
結果は `自力AC` `解説AC` `未AC`、分類は `知識不足` `考察不足` `実装力不足` `速度不足`
（自力ACなら分類は省略）。

**この記録が出題を進める。** 精選100問は 3 問ずつ出て、2 問ぶん書くと次の 3 問に移る。
解けなかった日も `未AC` で 1 行書けば進む。`bin/pick --random` のときは、
直近 10 行から出題難易度を自動調整する。

擬似コードを Claude に Rust へ変換させて解いた回は、結果の後ろに `(擬似)` を付ける
（例: `自力AC(擬似)`）。考察は自分でも実装は自分で書いていないので、
これが増えすぎていないかを週次で見るため。

2026-09-05 ABC199 C  解説AC  [考察不足] 方針は大体合っていたが細部が違った
2026-09-06 ABC051 B  自力AC  ３重ループを２重にした。早期continueは無くても良かった。
